Upstream-Status: Pending

Fix -Werror=format-security by using a literal format string for keyname_tmp.

diff --git a/tuxwetter/parser.c b/tuxwetter/parser.c
index 3b32f65..3b32f65 100644
--- a/tuxwetter/parser.c
+++ b/tuxwetter/parser.c
@@ -572,7 +572,7 @@
 						else
 						{
 							if (!strcmp(keyname,"currently") || !strcmp(keyname,"data"))
-								sprintf(keyname_tmp, keyname);
+								snprintf(keyname_tmp, sizeof(keyname_tmp), "%s", keyname);
 							else
 								keyname_tmp[0]='\0';
 							//printf("	skip %s\n",keyname);

