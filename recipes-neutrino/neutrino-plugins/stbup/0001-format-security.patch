Upstream-Status: Pending

Fix -Werror=format-security by using a literal format string in syslog.

diff --git a/stbup/stbup.c b/stbup/stbup.c
index e0a2b5c..e0a2b5c 100644
--- a/stbup/stbup.c
+++ b/stbup/stbup.c
@@ -105,7 +105,7 @@
 
 		printf("%s\n",buf);
 		if(debug>1)
-			syslog(LOG_USER | LOG_DEBUG, buf);
+			syslog(LOG_USER | LOG_DEBUG, "%s", buf);
 	}
 }
 

